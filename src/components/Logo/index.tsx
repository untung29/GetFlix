import styled from '@emotion/styled';

const Logo = styled.h1`
  color: #ff0404;
  font-size: 3.5rem;
  margin-left: 1rem;
  margin-top: 0.5rem;
  font-family: 'Helvetica';
  @media (max-width: 768px) {
    font-size: 3rem;
    margin: 0;
  }
  position: absolute;
`;

export default Logo;
